# 開発ガイドライン

## コーディング規約

### ハードコーディング禁止

以下はソースコードに直接書かない。

- API キー / シークレット → `.env`（`.gitignore` 対象）または Secret Manager / Vault 経由
- 外部サービスの URL / エンドポイント → 環境変数（`VITE_*`, `NEXT_PUBLIC_*`, `process.env.*`, `os.getenv` 等）
- ホスト・ポート・パス → `.env` または設定ファイル
- AWS アカウント ID / リソース ARN → 環境変数 + IAM Role 経由
- DB 接続文字列 → 環境変数（`DATABASE_URL` 等）
- ドキュメント内の機密情報 → プレースホルダ（`<API_ID>`, `<REDACTED>` 等）

### 言語別の規約

#### {{言語 A（例: TypeScript / JavaScript）}}

- {{命名規則（camelCase 関数 / PascalCase 型 / SCREAMING_SNAKE_CASE 定数）}}
- {{import 順序（標準ライブラリ → 外部 → 内部 → 相対パス）}}
- {{型定義方針（`type` vs `interface`、`any` 禁止 等）}}
- {{エラーハンドリング（Result 型 / try-catch / Error クラス継承）}}
- {{Lint / Formatter: ESLint + Prettier / Biome 等}}

#### {{言語 B（例: Python）}}

- {{命名規則（PEP 8 準拠）}}
- {{型ヒント方針（mypy strict / pyright）}}
- {{エラーハンドリングの方針}}
- {{Lint / Formatter: Ruff + Black 等}}

### セキュリティ

- コード変更後はセキュリティを考慮したコーディングを行う（XSS、CSRF、SQL インジェクション、コマンドインジェクション、SSRF 対策）
- 入力サイズ上限を必ず設ける（DoS 対策）
- ユーザー入力は必ずサニタイズ・バリデーション（クライアント側 + サーバー側の二重チェック）
- 認証・認可の境界を明確化（middleware で集中管理）

---

## Git 規約

### ブランチ戦略

- {{main / develop / feature ブランチの運用（GitHub Flow / Git Flow 等）}}
- {{命名規則（`feature/xxx`, `fix/xxx`, `chore/xxx` 等）}}

### コミットメッセージ

- {{プレフィックス規約（feat / fix / docs / chore / refactor / test / 等、Conventional Commits 推奨）}}
- {{1 行サマリ（50 文字以内）+ 詳細の書き方}}

### コミット前のセキュリティレビュー

- コミット前にクルトワ（security-engineer エージェント）にレビュー依頼（CLAUDE.md 必須ルール）
- API キー・URL・シークレットのハードコーディングがないか確認
- `.env` ファイルは `.gitignore` に必ず含める
- Critical / High 指摘があれば修正してからコミット

### Pull Request

- {{PR テンプレートの利用}}
- {{レビュー必須人数 / マージ条件（CI 通過 + N 人承認）}}

---

## テスト規約

### テストの品質

- 実際の機能を検証すること（`expect(true).toBe(true)` のような無意味なアサーション禁止）
- モックは必要最小限に留め、実際の動作に近い形でテストする
- 境界値・異常系・エラーケースも必ずテストする

### テスト分類

| レイヤ | ツール例 | 対象 |
|-------|---------|------|
| ユニット | Vitest / Jest / Pytest | 純粋関数・ロジック単体 |
| コンポーネント | React Testing Library / Vue Test Utils | UI コンポーネント |
| 統合 | Supertest / TestClient | API エンドポイント |
| E2E | Playwright / Cypress | ユーザーフロー |

### ハードコーディング禁止

- テストを通すためだけのハードコードは禁止
- 本番コードに `if (testMode)` のような条件分岐を入れない
- 環境変数や設定ファイルでテスト環境と本番環境を分離する

---

## ナレッジ蓄積ルール

作業中に得た知見・教訓は **発見した瞬間に即座に書き込む**。会話の最後やコミット前にまとめて書くのではなく、**発見した瞬間に書く**。

### 書き込み先

- 作業中の知見 → 該当する `.steering/[YYYYMMDD]-[開発タイトル]/` 内に記録（`decisions.md` 推奨）
- 検証済みの汎用的な知見 → 本ファイル（`docs/development-guidelines.md`）のドメイン別ルールに直接反映
- 横断的なハマりどころ → auto memory（`MEMORY.md` の `lessons/`）にも記録

### 即座に書き込むべきタイミング

- バグの根本原因が判明したとき
- 設計判断で選択肢を比較・決定したとき
- セキュリティレビューで指摘が見つかったとき
- 試行錯誤の末に解決策が判明したとき（失敗した試みも含めて）

### 記録すべき判断基準

記録する:
- 次回また同じことで迷いそうな判断
- {{プロジェクト固有の気づきにくい落とし穴}}
- 試行錯誤の末に判明した解決策（失敗した試みも含めて）
- 効果的だった手法・コマンド

記録しない:
- 公式ドキュメントに書いてあること
- コードを読めば自明な内容

---

## 図表・ダイアグラムの記載ルール

### 記載場所

設計図やダイアグラムは、関連する永続的ドキュメント内に直接記載する。
独立した diagrams フォルダは作成せず、メンテナンスコストを最小限に抑える。

### 記述形式

1. **Mermaid 記法（推奨）** — Markdown に直接埋め込め、バージョン管理が容易
2. **ASCII アート** — シンプルな図表に使用
3. **画像ファイル** — 複雑なワイヤフレームのみ。`docs/images/` 配下に PNG / SVG で配置

### 図表の更新

- 設計変更時は対応する図表も同時に更新
- 図表とコードの乖離を防ぐ

---

## ドメイン別ルール

> 本セクションには、プロジェクト固有のハマりどころ・運用ルールを蓄積する。
> 新しい知見が得られたら **該当ドメインの末尾に追記** する（CLAUDE.md「ナレッジ蓄積ルール」参照）。

### {{ドメイン A: フロントエンド}}

#### {{ルール名}}

{{背景・原因・対処法を簡潔に記述。
コミットハッシュ・関連 lessons へのリンクも残す。}}

### {{ドメイン B: バックエンド / API}}

#### {{ルール名}}

{{背景・原因・対処法}}

### {{ドメイン C: データベース / マイグレーション}}

#### {{ルール名}}

{{背景・原因・対処法}}

### {{ドメイン D: インフラ / CI/CD}}

#### {{ルール名}}

{{背景・原因・対処法}}

### {{ドメイン E: セキュリティ}}

#### {{ルール名}}

{{背景・原因・対処法}}

---

## 注意事項

- ドキュメントの作成・更新は段階的に行い、各段階で承認を得る
- `.steering/` のディレクトリ名は日付と開発タイトルで明確に識別できるようにする
- 永続的ドキュメントと作業単位のドキュメントを混同しない
- コード変更後は必ずドメイン別ガイドラインに従ったチェックを実施する
- 図表は必要最小限にとどめ、メンテナンスコストを抑える